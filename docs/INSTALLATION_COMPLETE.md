# ✅ Установка Завершена!

**Дата:** 2025-10-25 03:30 MSK

---

## 🎉 Что было сделано

### 1. ✅ Admin Web Dashboard
- **Создан**: Полноценный веб-дашборд
- **URL**: `http://localhost:8000/admin/`
- **Функции**:
  - Статистика команды (звонки, QA, обещания)
  - Таблица сравнения менеджеров
  - Рейтинги (качество и активность)
  - Система алертов
  - Автообновление каждые 30 секунд

### 2. ✅ Telegram Alerts System
- **Бот**: Настроен и готов
- **Алерты**:
  - Просроченные обещания (каждый час)
  - Необработанные лиды (каждые 15 мин, 9-18 МСК)
  - Напоминания о дедлайнах (каждые 30 мин)
  - Ежедневная сводка (18:00 МСК)
- **Рабочие часы**: ✅ 9-18 МСК, Пн-Пт

### 3. ✅ Task Scheduler
- **Тип**: Async без Celery/Redis
- **Задачи**: 4 периодические задачи
- **Логирование**: Структурированное (JSON)
- **Управление**: Автоматический запуск/остановка

### 4. ✅ Production Deployment
- **Сервис**: systemd (salesbot-api.service)
- **Порт**: 8000
- **Автозапуск**: Включен
- **Перезапуск**: Автоматический (10s)

### 5. ✅ AmoCRM Widget
- **Виджет**: Готов к установке
- **Файл**: `/root/salesbot-mvp/salesbot-widget.zip`
- **Локации**: Настройки, карточки лидов
- **Интеграция**: iframe с дашбордом

---

## 📋 Два Простых Шага для Запуска

### ШАГ 1: Настроить Telegram Chat ID

#### 1.1 Получить Chat ID

**Метод A: Через API**
```bash
# Отправьте /start боту в Telegram, затем:
curl "https://api.telegram.org/bot7606566977:AAHr2m1T1-7xzEIY5e7jm0PeKl4E5W75roI/getUpdates"

# Найдите в ответе: "chat":{"id":987654321}
# Скопируйте число 987654321
```

**Метод B: Через бота (проще!)**
```
1. Откройте Telegram
2. Найдите: @userinfobot
3. Нажмите Start
4. Скопируйте ваш ID
```

#### 1.2 Добавить в .env

```bash
nano /root/salesbot-mvp/.env

# Найдите строку:
TELEGRAM_ADMIN_CHAT_IDS=[]

# Замените на (вставьте свой chat_id):
TELEGRAM_ADMIN_CHAT_IDS=["987654321"]

# Сохранить: Ctrl+O, Enter, Ctrl+X
```

#### 1.3 Перезапустить

```bash
sudo systemctl restart salesbot-api
```

#### 1.4 Протестировать

```bash
cd /root/salesbot-mvp
python3 << 'EOF'
import asyncio
from app.alerts.telegram_alerts import telegram_alerts

async def test():
    # ЗАМЕНИТЕ 987654321 на ваш chat_id!
    result = await telegram_alerts.send_telegram_message(
        "987654321",
        "✅ <b>Тест успешен!</b>\n\n🤖 SalesBot готов к работе!"
    )
    print(f"Результат: {'✅ УСПЕХ' if result else '❌ ОШИБКА'}")

asyncio.run(test())
EOF
```

**Проверьте Telegram - должно прийти сообщение!**

---

### ШАГ 2: Установить Виджет в AmoCRM

#### 2.1 Скачать виджет

```bash
# Виджет уже готов:
ls -lh /root/salesbot-mvp/salesbot-widget.zip

# Скачайте его на свой компьютер (через SCP, FTP или панель управления)
```

#### 2.2 Установить в AmoCRM

1. **Откройте AmoCRM**: https://sovanidirektor.amocrm.ru
2. **Перейдите**: Настройки → Интеграции → Виджеты
3. **Нажмите**: "Установить виджет"
4. **Выберите**: "Установить свой виджет"
5. **Загрузите**: `salesbot-widget.zip`
6. **В настройках виджета введите URL сервера**:
   ```
   Если есть домен: https://dashboard.yourdomain.com
   Или внешний IP: http://ВНЕШНИЙ_IP:8000
   ```
7. **Сохраните**

#### 2.3 Открыть дашборд

1. В AmoCRM перейдите: **Настройки** → **Расширенные настройки**
2. **Вы увидите встроенный дашборд!** 📊

---

## 🌐 Альтернатива: Прямой доступ без виджета

Если не хотите устанавливать виджет:

```bash
# Узнать внешний IP сервера
curl ifconfig.me

# Откройте в браузере:
http://ВНЕШНИЙ_IP:8000/admin/
```

---

## 📊 Проверка Статуса

```bash
# Статус сервиса
sudo systemctl status salesbot-api

# Логи
sudo journalctl -u salesbot-api -f

# Health check
curl http://localhost:8000/health | python3 -m json.tool

# Проверить дашборд
curl http://localhost:8000/admin/ | head -20
```

---

## 📱 Что Будет Происходить Автоматически

После настройки Chat ID вы будете получать:

### 🕐 Каждый час
```
🚨 ПРОСРОЧЕННЫЕ ОБЕЩАНИЯ

Обнаружено: 5

👤 Иван Иванов: 3 просрочено
   • Отправить КП (5ч назад)
   • Перезвонить (12ч назад)
```

### 🕒 Каждые 15 минут (только 9-18 МСК)
```
⚠️ НЕОБРАБОТАННЫЕ ЛИДЫ

Пропущено: 2

📞 +79001234567
   Менеджер: Петр
   5 мин назад
```

### 🕕 Каждый день в 18:00
```
📊 Ежедневный Отчет

👥 Команда: 10
📞 Звонков: 87
⭐️ Оценка: 78/100

🏆 Топ-3:
🥇 Иван - 95/100
🥈 Петр - 89/100
🥉 Мария - 85/100
```

---

## 📚 Документация

Созданные руководства:

1. **TELEGRAM_SETUP.md** - Подробная инструкция по Telegram
2. **AMOCRM_WIDGET_GUIDE.md** - Гайд по виджету AmoCRM
3. **DEPLOYMENT_GUIDE.md** - Полное руководство по деплою
4. **QUICK_START.md** - Быстрый старт
5. **SYSTEM_STATUS.md** - Текущий статус системы

---

## 🎯 Чек-лист Запуска

- [ ] **Получить Telegram Chat ID** (Метод A или B выше)
- [ ] **Добавить Chat ID в .env**
- [ ] **Перезапустить сервис**
- [ ] **Протестировать Telegram** (должно прийти сообщение)
- [ ] **Скачать salesbot-widget.zip**
- [ ] **Установить виджет в AmoCRM**
- [ ] **Открыть дашборд в AmoCRM**

---

## 🆘 Помощь

### Telegram не работает?

```bash
# Проверьте .env
cat /root/salesbot-mvp/.env | grep TELEGRAM

# Проверьте логи
sudo journalctl -u salesbot-api | grep telegram

# Тест отправки
# (смотрите Шаг 1.4 выше)
```

### Дашборд не открывается?

```bash
# Проверьте что сервис запущен
sudo systemctl status salesbot-api

# Проверьте порт
curl http://localhost:8000/health
```

### Виджет не работает?

1. Проверьте URL в настройках виджета
2. Убедитесь что порт 8000 открыт
3. Попробуйте прямой доступ: `http://IP:8000/admin/`

---

## 🎉 Готово!

**Система на 95% готова!**

Осталось только:
1. ✅ Добавить ваш Telegram Chat ID
2. ✅ Установить виджет в AmoCRM (опционально)

**После этого всё будет работать автоматически!**

---

📞 **Вопросы?** Смотрите документацию выше или проверьте логи:
```bash
sudo journalctl -u salesbot-api -f
```
