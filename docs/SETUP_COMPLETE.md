# 🎉 SalesBot MVP - Установка Завершена!

## ✅ Что настроено и работает:

### 1. **Интеграция с AmoCRM**
- **Subdomain:** `sovanidirektor.amocrm.ru`
- **Client ID:** `7f5c3b22-3919-4931-a107-3499a44ee49e`
- **Токены:** Access Token & Refresh Token получены и сохранены
- **Redirect URI:** `https://app.justbusiness.lol/auth/callback`

### 2. **OpenAI API**
- **API Key:** Настроен (взят из ai_seller проекта)
- **Модель:** gpt-3.5-turbo для анализа звонков

### 3. **Telegram Bot**
- **Token:** `7606566977:AAHr2m1T1-7xzEIY5e7jm0PeKl4E5W75roI`
- **Бот готов** к регистрации пользователей и отправке уведомлений

### 4. **API Сервис**
- **Локальный порт:** 8001
- **Доступ через HTTPS:** https://app.justbusiness.lol/health
- **Статус:** ✅ Работает
- **Автозапуск:** ✅ Настроен (systemd)

### 5. **База данных**
- **Тип:** SQLite (async)
- **Путь:** `/root/salesbot-mvp/salesbot.db`
- **Статус:** ✅ Инициализирована

### 6. **Redis**
- **Статус:** ✅ Работает
- **Используется для:** Кэширование результатов анализа

---

## 📡 Доступные API Endpoints:

### Через HTTPS (публичные):
- `https://app.justbusiness.lol/health` - Проверка здоровья системы
- `https://app.justbusiness.lol/metrics` - Метрики (требует API key)
- `https://app.justbusiness.lol/webhook/amocrm/call` - Webhook для AmoCRM
- `https://app.justbusiness.lol/api/*` - API методы

### Локально:
- `http://127.0.0.1:8001/` - Информация о сервисе
- `http://127.0.0.1:8001/docs` - Swagger документация (только в dev режиме)

---

## 🔧 Управление сервисом:

```bash
# Проверить статус
systemctl status salesbot-api

# Перезапустить
systemctl restart salesbot-api

# Остановить
systemctl stop salesbot-api

# Запустить
systemctl start salesbot-api

# Посмотреть логи (в реальном времени)
journalctl -u salesbot-api -f

# Посмотреть последние 50 строк логов
journalctl -u salesbot-api -n 50
```

---

## 📋 Следующие шаги для запуска:

### ШАГ 1: Настроить Webhook в AmoCRM

1. Откройте AmoCRM: https://sovanidirektor.amocrm.ru
2. Перейдите в **Настройки** → **Интеграции**
3. Найдите вашу интеграцию **"SalesBot MVP"**
4. В разделе **"Webhooks"** добавьте:
   - **URL:** `https://app.justbusiness.lol/webhook/amocrm/call`
   - **События:**
     - ✅ Звонок создан
     - ✅ Звонок обновлен
   - **Метод:** POST

### ШАГ 2: Запустить Telegram бота (опционально)

Бот можно запустить позже. Для запуска:

```bash
cd /root/salesbot-mvp
python3 -c "
import asyncio
from app.bot.telegram_bot import start_bot_background
asyncio.run(start_bot_background())
"
```

### ШАГ 3: Синхронизировать менеджеров из AmoCRM

```bash
curl -X POST "https://app.justbusiness.lol/api/sync?sync_type=managers"
```

### ШАГ 4: Тестовый звонок

После настройки webhook:
1. Сделайте тестовый звонок в AmoCRM
2. Убедитесь, что у звонка есть запись
3. Система автоматически:
   - Скачает запись
   - Расшифрует через Whisper
   - Проанализирует через GPT
   - Отправит уведомление в Telegram (если настроен)

---

## 🔍 Проверка работоспособности:

### Тест API:
```bash
curl -s https://app.justbusiness.lol/health | jq
```

Ожидаемый результат:
```json
{
  "status": "healthy" или "unhealthy",
  "checks": {
    "redis": { "status": "healthy" },
    "database": { "status": "healthy" },
    ...
  }
}
```

### Тест AmoCRM подключения:
```bash
curl -s https://app.justbusiness.lol/api/sync?sync_type=managers
```

---

## 📂 Важные файлы и пути:

- **Конфигурация:** `/root/salesbot-mvp/.env`
- **Логи:** `/var/log/salesbot/` и `journalctl -u salesbot-api`
- **База данных:** `/root/salesbot-mvp/salesbot.db`
- **Аудио файлы:** `/tmp/salesbot/audio/`
- **Systemd service:** `/etc/systemd/system/salesbot-api.service`
- **Nginx config:** `/etc/nginx/sites-available/sovani-miniapp`

---

## 🐛 Решение проблем:

### API не отвечает:
```bash
systemctl status salesbot-api
journalctl -u salesbot-api -n 50
```

### Ошибка "Invalid host header":
Убедитесь, что в `/root/salesbot-mvp/app/main.py` в `TrustedHostMiddleware` указан `app.justbusiness.lol`

### AmoCRM не подключается:
1. Проверьте токены в `.env`
2. Токен действует 24 часа, после чего автоматически обновляется через refresh_token

### OpenAI API ошибки:
Проверьте лимиты на аккаунте OpenAI: https://platform.openai.com/usage

---

## 📊 Мониторинг:

### Метрики системы:
```bash
curl -s "https://app.justbusiness.lol/metrics?api_key=metrics_key_123" | jq
```

### Статистика очереди задач:
```bash
curl -s https://app.justbusiness.lol/api/queue/stats | jq
```

---

## 🎯 Что делает система:

1. **Получает webhook** от AmoCRM о новом звонке
2. **Скачивает запись** звонка
3. **Обрабатывает аудио** (конвертирует, нормализует)
4. **Расшифровывает** через OpenAI Whisper API
5. **Анализирует** качество звонка через GPT-3.5-turbo:
   - Оценка по 5 критериям
   - Сильные и слабые стороны
   - Рекомендации
   - Анализ возражений
   - Настроение клиента
6. **Отправляет результаты** в Telegram менеджеру
7. **Сохраняет все** в базе данных

---

## 🔐 Безопасность:

- ✅ HTTPS через Let's Encrypt
- ✅ Токены AmoCRM зашифрованы
- ✅ JWT для API аутентификации
- ✅ Rate limiting для защиты от abuse
- ✅ TrustedHost middleware

---

## 📞 Поддержка:

Если возникли вопросы:
1. Проверьте логи: `journalctl -u salesbot-api -f`
2. Проверьте статус: `systemctl status salesbot-api`
3. Проверьте health check: `curl https://app.justbusiness.lol/health`

---

## ✅ Чек-лист готовности:

- [x] AmoCRM интеграция создана
- [x] Токены получены и сохранены
- [x] OpenAI API key настроен
- [x] Telegram bot token настроен
- [x] База данных инициализирована
- [x] API сервис запущен
- [x] Nginx настроен
- [x] Systemd автозапуск настроен
- [ ] Webhook в AmoCRM настроен (нужно сделать вручную)
- [ ] Менеджеры синхронизированы
- [ ] Telegram бот запущен (опционально)

---

**Система готова к работе! 🚀**

Дата установки: 24.10.2025
