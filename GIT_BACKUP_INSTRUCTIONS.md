# 📦 Инструкция по резервному копированию на GitHub

## ✅ Уже сделано

1. ✅ Инициализирован Git репозиторий в `/root/salesbot-mvp`
2. ✅ Создан `.gitignore` (исключены чувствительные данные: .env, *.db, recordings/)
3. ✅ Сделан первый коммит со всем кодом (99 файлов)
4. ✅ Создан `CHANGELOG.md` с полной историей изменений

## 📝 Что нужно сделать для пуша на GitHub

### Вариант 1: Создать новый репозиторий на GitHub (рекомендуется)

1. **Создайте новый репозиторий на GitHub:**
   - Откройте https://github.com/new
   - Название: `salesbot-mvp`
   - Описание: `AI-powered B2B sales call analysis system with AmoCRM integration`
   - Видимость: Private (рекомендуется) или Public
   - ❌ НЕ выбирайте "Initialize this repository with a README"
   - Нажмите "Create repository"

2. **Получите новый GitHub Personal Access Token:**
   - Откройте https://github.com/settings/tokens/new
   - Note: `salesbot-mvp-backup`
   - Expiration: 90 days (или No expiration)
   - Выберите права: `repo` (Full control of private repositories)
   - Нажмите "Generate token"
   - **СОХРАНИТЕ токен!** Он показывается только один раз

3. **Запустите команды на сервере:**

```bash
cd /root/salesbot-mvp

# Добавьте remote (замените YOUR_TOKEN и YOUR_USERNAME)
export GITHUB_TOKEN="YOUR_TOKEN_HERE"
git remote add origin https://$GITHUB_TOKEN@github.com/YOUR_USERNAME/salesbot-mvp.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Запушьте код
git push -u origin main
```

### Вариант 2: Использовать SSH (если настроен SSH ключ)

```bash
cd /root/salesbot-mvp
git remote add origin git@github.com:YOUR_USERNAME/salesbot-mvp.git
git branch -M main
git push -u origin main
```

## 🔄 Регулярное резервное копирование

После первого пуша, для обновления репозитория:

```bash
cd /root/salesbot-mvp

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Запушить
git push
```

### Автоматизация (опционально)

Создайте cron задачу для автоматического бэкапа каждый день в 3:00:

```bash
# Открыть crontab
crontab -e

# Добавить строку:
0 3 * * * cd /root/salesbot-mvp && git add . && git commit -m "Auto backup $(date +\%Y-\%m-\%d)" && git push origin main >> /var/log/git-backup.log 2>&1
```

## 📋 Что включено в репозиторий

### Исключено (.gitignore):
- ❌ .env файлы (содержат секреты)
- ❌ Базы данных (*.db, *.sqlite)
- ❌ Записи звонков (recordings/)
- ❌ Логи (*.log)
- ❌ Временные файлы Python (__pycache__, *.pyc)

### Включено:
- ✅ Весь исходный код (app/)
- ✅ Конфигурационные файлы
- ✅ Документация (*.md)
- ✅ Тесты (tests/)
- ✅ Шаблоны (templates/)
- ✅ Зависимости (requirements.txt)
- ✅ Миграции БД (migrations/)
- ✅ Виджет AmoCRM (widget/)

## 🔐 Безопасность

**ВАЖНО:** Никогда не коммитьте:
- Токены и пароли (.env)
- Базы данных с реальными данными
- Записи звонков клиентов
- API ключи
- SSH ключи

Если случайно закоммитили чувствительные данные:

```bash
# НЕМЕДЛЕННО измените все токены/пароли!
# Затем удалите файл из истории:
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch PATH/TO/SENSITIVE/FILE" \
  --prune-empty --tag-name-filter cat -- --all

# Форс-пуш (ОПАСНО! Делайте только если уверены!)
git push origin --force --all
```

## 📊 Текущий статус

```bash
# Проверить статус
cd /root/salesbot-mvp
git status

# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения
git diff
```

## 💾 Дополнительные бэкапы

Помимо GitHub, рекомендуется также:

1. **Локальный бэкап на другом сервере:**
```bash
rsync -avz --exclude='recordings/' --exclude='*.db' \
  /root/salesbot-mvp/ \
  backup-server:/backups/salesbot-mvp/
```

2. **Tar архив:**
```bash
cd /root
tar -czf salesbot-mvp-backup-$(date +%Y%m%d).tar.gz \
  --exclude='salesbot-mvp/recordings' \
  --exclude='salesbot-mvp/*.db' \
  --exclude='salesbot-mvp/__pycache__' \
  salesbot-mvp/
```

3. **Cloud storage (опционально):**
   - AWS S3
   - Google Cloud Storage
   - Yandex Object Storage
   - Dropbox
